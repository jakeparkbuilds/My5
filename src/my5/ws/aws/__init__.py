# AWS WebSocket backend: DynamoDBRegistry, ApiGwSender, Lambda handlers.
# Swap these for the local equivalents (Registry, LocalSender) without
# touching push_progress, job_record_to_message, or any handler logic.
